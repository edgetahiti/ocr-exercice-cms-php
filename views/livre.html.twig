{% extends "layout.html.twig" %}

{% block title %}{{ livre.title }}{% endblock %}

{% block content %}
<p>
    <h2>{{ livre.title }}</h2>
    <p>{{ livre.summary }}</p>
    <h3>Comments</h3>
    {% for author in authors %}
        <strong>{{ auth_first_name.author }}</strong> said : {{ auth_last_name.content }}<br>
    {% else %}
        No comments yet.
    {% endfor %}
</p>
{% endblock %}
